<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eugeneprogram.comment.dao.CommentMapper">
    <!-- 댓글 조회 -->
    <select id="commentList" resultType="java.util.Map">
        SELECT cmt_id AS cmtId, cmt_parent AS cmtParent, cmt_text AS cmtText, cmt_create_date AS cmtCreateDate
        FROM cmt
        ORDER BY cmt_create_date DESC
    LIMIT #{offset}, #{limit}
    </select>
    <select id="commentListLatest" resultType="java.util.Map">
        SELECT cmt_id AS cmtId, cmt_parent AS cmtParent, cmt_text AS cmtText, cmt_create_date AS cmtCreateDate
        FROM cmt
        ORDER BY cmt_create_date DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="commentListOldest" resultType="java.util.Map">
        SELECT cmt_id AS cmtId, cmt_parent AS cmtParent, cmt_text AS cmtText, cmt_create_date AS cmtCreateDate
        FROM cmt
        ORDER BY cmt_create_date ASC
        LIMIT #{offset}, #{limit}
    </select>
    <select id="getCommentById" parameterType="long" resultType="java.util.Map">
        SELECT cmt_id AS cmtId, cmt_parent AS cmtParent, cmt_text AS cmtText, cmt_create_date AS cmtCreateDate, pst_id AS pstId
        FROM cmt
        WHERE cmt_id = #{cmtId}
    </select>

    <!-- 댓글 작성 -->
    <insert id="insertComment" parameterType="java.util.Map">
        INSERT INTO cmt(cmt_parent, cmt_text, cmt_create_date, mb_id, pst_id)
        VALUES(1, #{cmtText}, #{cmtCreateDate}, 1, 4)
    </insert>
    
    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="java.util.Map">
        UPDATE cmt
        SET cmt_text = #{cmtText},
        mb_id = 1,
        cmt_parent=1,
        pst_id = 4
            <!--cmt_create_date = now()  현재 시간을 기록하도록 설정-->
        WHERE cmt_id = #{cmtId}
    </update>
    
    <!-- 댓글 삭제 -->
    <delete id="deleteComment" parameterType="java.util.Map">
        DELETE FROM cmt
        WHERE cmt_id = #{cmtId}
    </delete>
    <!--좋아요 관련-->
    <select id="likeList" resultType="java.util.Map">
        SELECT li_id AS liId, cmt_id AS cmtID, mb_id AS mbId
        FROM `like`
    </select>
     <!-- 특정 댓글과 사용자에 대한 좋아요 조회 -->
    <select id="getLikeByCmtIdAndMbId" parameterType="map" resultType="java.util.Map">
        SELECT lk_id AS lkId, cmt_id AS cmtId, mb_id AS mbId
        FROM `like`
        WHERE cmt_id = #{cmtId} AND mb_id = 1
        LIMIT 1
    </select>
     <!-- 좋아요 추가 -->
    <insert id="insertLike" parameterType="java.util.Map">
        INSERT INTO `like` (cmt_id, mb_id)
        VALUES (#{cmtId}, 1)
    </insert>
    
    <!-- 좋아요 삭제 -->
    <delete id="deleteLike" parameterType="java.util.Map">
        DELETE FROM `like`
        WHERE cmt_id = #{cmtId} AND mb_id = 1
    </delete>
    <!-- 특정 댓글의 좋아요 수 조회 -->
    <select id="countLikes" parameterType="long" resultType="int">
        SELECT COUNT(*)
        FROM `like`
        WHERE cmt_id = #{cmtId}
    </select>
    <select id="countTotalComments" resultType="int">
    SELECT COUNT(*)
    FROM cmt
    </select>    
</mapper>
